import SignupContent from "./content";

export const metadata = {
    title: "Sign Up - PassLock"
};

const SignupPage = () => {
    return (
        <SignupContent />
    )
};

export default SignupPage;