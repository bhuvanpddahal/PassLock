import SigninContent from "./content";

export const metadata = {
    title: "Sign In - PassLock"
};

const SigninPage = () => {
    return (
        <SigninContent />
    )
};

export default SigninPage;