import SearchContent from "./content";

export const metadata = {
    title: "Search - PassLock"
};

const SearchPage = () => {
    return (
        <SearchContent />
    )
};

export default SearchPage;