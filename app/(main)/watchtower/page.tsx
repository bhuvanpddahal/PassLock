import WatchtowerContent from "./content";

export const metadata = {
    title: "Watchtower - PassLock"
};

const WatchtowerPage = () => {
    return (
        <WatchtowerContent />
    )
};

export default WatchtowerPage;