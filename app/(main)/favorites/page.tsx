import FavoritesContent from "./content";

export const metadata = {
    title: "Favorites - PassLock"
};

const FavoritesPage = () => {
    return (
        <FavoritesContent />
    )
};

export default FavoritesPage;